services:
  - type: web
    name: infynux-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start:render
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: --dns-result-order=ipv4first
